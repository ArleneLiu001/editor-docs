# WPSweboffice基础功能罗列参考

支持在线创建、编辑提供与本地WPS Office高度相似的编辑界面和基础功能（字体、段落、样式、公式、图表、图片插入、基本动画等）。

1. 文档侧边栏（最多四层）
  是否开启目录功能，默认开启
  打开文档时，默认以最佳比例显示
2. 查询文本位置和进行文本替换
3. 粘贴带格式的html数据到指定的区域。
``
（在wps weboffice中，
首先获取选中区域，再使用PasteHtml就能实现。）
``
 思考？在自己的组件库能不能封装下此功能。
4. 搜索的时候，搜索的文字高亮实现。
5. 文档内容提取和批量填充：文档合并，插入等
 思考？（能不能使用wangeditor也封装一个方法，直接可以插入）

6. 开启了限制编辑模式，打开模板文档后，我们发现除了内容控件可以编辑外，其它地方的内容都变为了只读。
7. 在光标处插入内容控件（可不可以一行代码，在光标处插入内容控件。
```
例如封装一个add方法，
 const app = instance.Application

  // 内容控件对象
  const contentControls = await app.ActiveDocument.ContentControls

  // 在光标处插入内容控件
  await contentControls.Add()
```

8. 超链接跳转。
9. 评论。
10. 统计单词数量。
11. 缩放。
12. 目录。

